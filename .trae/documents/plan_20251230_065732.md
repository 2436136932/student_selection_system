根据用户建议，直接在carousel表中添加切换时间字段，实现轮播图切换时间设置和插入3张初始轮播图：

**1. 数据库修改**

* 在carousel表中添加interval字段，用于存储轮播图切换时间（单位：毫秒）

**2. 后端修改**

* 更新Carousel实体类，添加interval字段

* 更新CarouselMapper和相关XML文件

* 更新CarouselService和Controller，支持interval字段的CRUD操作

**3. 前端修改**

* 更新CarouselManage.vue，添加轮播图切换时间设置功能

* 更新HomeView\.vue，使用轮播图的interval字段作为切换时间

**4. 数据初始化**

* 在database\_init.sql中插入3张初始轮播图数据，包含interval字段

**具体实现步骤：**

1. 修改carousel表结构，添加interval字段
2. 更新后端Carousel实体类和相关代码
3. 更新前端轮播图管理页面，添加切换时间设置
4. 更新前端轮播图展示组件，使用动态切换时间
5. 插入3张初始轮播图数据
6. 测试功能是否正常工作

**注意事项：**

* interval字段单位为毫秒，默认值建议设置为3000毫秒

* 轮播图管理页面中，切换时间设置应使用秒为单位，方便用户操作

* 确保前后端数据类型一致

