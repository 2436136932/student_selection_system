# 删除评奖标准管理功能的执行计划

## 一、删除后端代码

### 1. Controller层
- 删除 `StandardController.java`
- 删除 `SelectionCriteriaController.java`

### 2. Service层
- 删除 `StandardService.java` 接口
- 删除 `StandardServiceImpl.java` 实现类
- 删除 `SelectionCriteriaService.java` 接口
- 删除 `SelectionCriteriaServiceImpl.java` 实现类

### 3. Repository/Mapper层
- 删除 `StandardRepository.java`
- 删除 `SelectionCriteriaMapper.java`

### 4. Entity层
- 删除 `Standard.java` 实体类
- 删除 `SelectionCriteria.java` 实体类

### 5. 检查其他引用
- 检查 `Award.java` 等相关实体类，删除对评奖标准的引用
- 检查其他Service类，删除对评奖标准服务的引用

## 二、删除前端代码

### 1. 查找并删除相关组件
- 查找与评奖标准相关的Vue组件
- 删除相关的路由配置
- 删除相关的API调用

## 三、修改数据库初始文件

### 1. 修改 `database_init.sql`
- 删除 `selection_criteria` 表的创建语句
- 删除 `standards` 表的创建语句
- 删除 `selection_criteria` 表的TRUNCATE语句

## 四、初始化数据库

### 1. 停止现有服务
- 停止后端Spring Boot服务
- 停止前端Vue服务

### 2. 执行数据库初始化
- 运行修改后的 `database_init.sql` 文件，重新初始化数据库

### 3. 重启服务
- 重启后端Spring Boot服务
- 重启前端Vue服务

## 五、验证功能

### 1. 验证删除结果
- 确认评奖标准管理功能已从系统中移除
- 验证其他功能正常运行
- 验证数据库结构正确

## 六、注意事项

- 确保所有引用都已删除，避免编译错误
- 确保数据库初始化脚本正确执行
- 确保系统其他功能不受影响
- 备份重要数据，防止意外丢失