1. **分析当前布局**：用户名和密码输入框被包裹在 `.input-field` 类中，该类定义了椭圆形胶囊样式，内部包含图标和输入框元素。

2. **问题定位**：

   * `.input-field` 是 flex 容器，包含图标和输入框

   * 输入框已有 `flex: 1` 和 `width: 100%`，但可能受 Element Plus 内部样式影响

   * 图标有 `margin-right: 15px`，但这是合理的间距

3. **解决方案**：

   * 确保 `.input-field` 内部布局正确，输入框占据剩余空间

   * 调整 Element Plus 输入框内部样式，移除可能的宽度限制

   * 优化输入框的 `padding` 和 `margin`，确保完全填满胶囊

4. **具体修改**：

   * 修改 `.no-border-input` 相关样式，确保输入框完全填充父容器

   * 检查并调整 `.input-field` 内部元素的对齐方式

   * 测试登录和注册表单的输入框效果

5. **预期效果**：

   * 用户名和密码输入框完全填满椭圆形胶囊

   * 保持图标和输入文字的合理间距

   * 不影响其他表单元素的布局

