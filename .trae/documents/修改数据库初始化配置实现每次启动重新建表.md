1. **修改application.yml配置文件**：将spring.sql.init.mode从never改为always
2. **配置说明**：

   * spring.sql.init.mode=always会导致每次启动应用时都执行数据库初始化脚本

   * database\_init.sql脚本已经包含完整的删除现有表、创建新表和插入初始数据的逻辑

   * 该脚本会先禁用外键约束，按依赖关系删除所有表，然后重新创建表并插入初始数据
3. **预期效果**：每次启动后端应用时，都会自动执行database\_init.sql脚本，实现数据库表的完全重建和数据初始化

