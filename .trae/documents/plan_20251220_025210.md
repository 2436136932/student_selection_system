1. **修改ChatController类**：
   - 注入TeacherService依赖
   - 保持StudentService依赖（已存在）
   - 修改getAllUsersExceptCurrent()方法，为不同角色用户添加额外信息

2. **为教师用户添加学院信息**：
   - 在用户列表接口中，对于角色为'teacher'的用户，调用findTeacherByUserId()方法获取教师信息
   - 将教师的department字段添加到返回结果中
   - 字段名使用'department'，与前端预期一致

3. **为学生用户添加专业和班级信息**：
   - 在用户列表接口中，对于角色为'student'的用户，调用findStudentByUserId()方法获取学生信息
   - 将学生的major和class_name字段添加到返回结果中
   - 字段名使用'major'和'className'，与前端预期一致

4. **修改getCurrentUserChatSessions()方法**：
   - 同样为会话列表中的用户添加学院/专业班级信息
   - 确保聊天会话列表中也能显示完整的用户信息

5. **保持向后兼容性**：
   - 所有新增字段都添加默认值处理
   - 确保数据为空时不会导致前端报错

具体修改文件：
- `ChatController.java`：注入TeacherService，修改两个方法来添加额外用户信息
