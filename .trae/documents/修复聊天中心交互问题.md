1. **修复关闭会话功能**：

   * 在 `ChatWindow.vue` 中，为 `handleCloseSession` 方法添加事件发射，通知父组件关闭会话

   * 在 `ChatView.vue` 中添加关闭会话的处理逻辑，清除当前会话和消息

2. **修复切换用户没反应的问题**：

   * 统一会话对象的属性名使用：将 `ChatList.vue` 中的 `isUserActive` 方法修改为使用 `studentId` 和 `teacherId` 进行判断，与其他组件保持一致

3. **确保组件间事件通信正常**：

   * 检查 `ChatWindow.vue` 是否正确定义了关闭会话的事件

   * 在 `ChatView.vue` 中添加对关闭会话事件的监听

4. **修复UI反馈问题**：

   * 确保用户点击切换时，活跃状态正确显示

   * 关闭会话后，聊天窗口区域显示"请选择一个用户开始聊天"

具体修改文件：

* `ChatWindow.vue`：添加关闭会话事件发射

* `ChatView.vue`：添加关闭会话处理逻辑，监听关闭事件

* `ChatList.vue`：修复 `isUserActive` 方法的属性名匹配问题

