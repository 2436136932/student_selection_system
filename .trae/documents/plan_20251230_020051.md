### 实现计划

#### 任务1：修改菜单栏的"成绩管理"为"评分管理"

1. **修改 App.vue 中的菜单定义**
   - 文件路径：`G:\javacode\student-selection-system\frontend\src\App.vue`
   - 修改内容：将第185行的 `text: '成绩管理'` 改为 `text: '评分管理'`

2. **修改 SystemSettingsView.vue 中的菜单配置**
   - 文件路径：`G:\javacode\student-selection-system\frontend\src\views\SystemSettingsView.vue`
   - 修改内容：
     - 将第219行的 `text: '成绩管理'` 改为 `text: '评分管理'`
     - 将第289行的 `text: '成绩管理'` 改为 `text: '评分管理'`

#### 任务2：为评分统计添加可视化图形

1. **修改 ScoreView.vue**
   - 文件路径：`G:\javacode\student-selection-system\frontend\src\views\ScoreView.vue`
   - 修改内容：
     - 添加 ECharts 图表容器，用于展示评分等级分布
     - 导入 ECharts 相关组件和函数
     - 添加图表初始化和数据更新逻辑

2. **添加图表容器**
   - 在评分统计卡片中添加一个 `div` 元素作为图表容器
   - 设置合适的宽度和高度，确保图表清晰可见
   - 使用 `ref` 引用图表容器，方便初始化 ECharts 实例

3. **实现图表逻辑**
   - 在 `onMounted` 钩子中初始化 ECharts 实例
   - 添加 `statisticsChart` 响应式变量，存储图表实例
   - 修改 `getStatistics` 函数，获取评分等级分布数据
   - 添加 `updateChart` 函数，用于更新图表数据

4. **设计图表样式**
   - 使用饼图展示各个等级的数量分布
   - 设置图表标题、图例、数据标签等
   - 确保图表颜色美观，与整体页面风格一致

### 预期效果

1. 菜单栏中的"成绩管理"将显示为"评分管理"
2. 点击评分统计的"查询统计"按钮后，除了显示总课程数、有效成绩数和平均分外，还会显示一个饼图
3. 饼图将直观展示该学生获得的各个等级（A、B、C、D、F）的数量分布
4. 图表支持交互功能，如悬停显示详情、点击图例切换显示等

### 技术实现

- 使用 ECharts 6.0.0 实现可视化图表
- 利用现有的 Axios 调用获取评分等级分布数据
- 使用 Vue 3 的响应式系统管理图表数据
- 保持与现有代码风格一致，确保良好的可维护性